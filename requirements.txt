# Core ML + GPU simulation
# NVIDIA Warp: install as `warp-lang` on PyPI (`import warp`). Do NOT `pip install warp` (wrong package).
warp-lang>=1.3.0
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
Pillow>=10.0.0
tqdm>=4.66.0
scikit-learn>=1.3.0
matplotlib>=3.8.0
